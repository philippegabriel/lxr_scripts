# Fetch repos
# Run the LXR indexer
# m h  dom mon dow   command
PATH=/bin:/usr/bin:/usr/sbin
LOG=/tmp/cronrun.webalizer
#webalizer options config at /etc/webalizer/webalizer.conf
00 */1 * * * sudo webalizer > $LOG 2>&1

